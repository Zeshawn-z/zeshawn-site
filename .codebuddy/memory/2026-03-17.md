# 2026-03-17

## 后台 Markdown 在线编辑器

为后台博客编辑器添加了分屏实时预览 Markdown 编辑器，替换原来的纯 `<textarea>`。

- **安装依赖**：`@uiw/react-md-editor`（v4.0.11）
- **`src/components/MdEditor.tsx`** — 封装编辑器组件
  - 使用 `next/dynamic` 动态导入（SSR 不兼容）
  - 公式预览通过 `remark-math` + `rehype-katex` 插件链，与页面渲染管线完全一致（不再用自定义 `code` 组件方式）
  - 同时配置 `remark-gfm` 插件支持 GFM 表格、任务列表等
  - 通过 MutationObserver 监听 `<html>` 的 `.dark` class 变化，同步 `data-color-mode` 属性实现暗色模式跟随
  - 默认高度 600px，可拖拽调整
- **`src/app/admin/page.tsx`** — PostsEditor 中的 textarea 替换为 `<MdEditor>` 组件
- TypeScript 类型检查通过 ✅

## GitHub Actions 工作流

- **`.github/workflows/build.yml`** — master 分支推送时自动构建打包
  - 触发条件：`push` to `master`
  - Node.js 20 + `npm ci`
  - `npm run build`（Next.js standalone 模式，`next.config.ts` 已配置 `output: 'standalone'`）
  - 组装 standalone 产物：复制 `.next/static` 和 `public` 到 standalone 目录
  - 打包为 `zeshawn-site.tar.gz`
  - 上传为 GitHub Actions artifact（保留 30 天）

## 博客评论功能（含楼中楼、分页、排序）

为博客文章添加了完整评论系统，无需登录，只需昵称即可评论。

### 数据库层
- **`src/lib/schema.ts`** — `comments` 表：id, post_slug, parent_id（楼中楼）, floor（主评论楼数）, nickname, content, created_at
- **`src/lib/db.ts`** — 建表 SQL + `idx_comments_post_slug` + `idx_comments_parent_id` 索引
- **`src/lib/data.ts`** — 评论 CRUD：
  - `getCommentsBySlug(slug, {page, limit, order})` — 分页查主评论 + 批量加载回复
  - `addComment(slug, nickname, content, parentId?)` — 发表评论，主评论自动计算楼数
  - `deleteComment(id)` — 删除评论 + 级联删回复
  - `deleteCommentsBySlug(slug)` — 级联删某文章全部评论
  - `deletePost()` 已改为先级联删评论再删文章

### API 路由
- **`/api/comments`** GET — 按 slug 获取评论，支持 `page` 和 `order` 参数（10条/页）
- **`/api/comments`** POST — 发表评论/回复，支持 `parentId` 参数
- **`/api/admin/comments`** GET — 管理员获取全部评论，支持 `slug` 筛选
- **`/api/admin/comments/[id]`** DELETE — 管理员删评论

### 前端 BlogComments
- 楼数显示（#1, #2...），回复不计入楼数
- 时间排序切换（最早优先 / 最新优先）
- 分页导航（10条/页，上/下一页 + 页码）
- 楼中楼回复：每条主评论有"回复"按钮，展开内嵌回复输入框
- 回复列表缩进展示在主评论下方

### 后台管理
- 博客列表每行增加 💬 查看评论按钮 → 跳转评论 Tab 并自动筛选该文章
- 评论管理支持按文章 slug 筛选 + 清除筛选
- 回复条目左侧缩进 + "回复"标签区分
- 删除评论会级联删除回复

## 数据库迁移修复

- **问题**：已有 `comments` 表缺少 `parent_id` 和 `floor` 列，`CREATE TABLE IF NOT EXISTS` 不会修改已有表结构，导致 `SqliteError: no such column: parent_id`
- **修复**：在 `src/lib/db.ts` 的建表语句之后增加迁移逻辑，逐条执行 `ALTER TABLE comments ADD COLUMN`，用 try-catch 忽略列已存在的错误
- 新库和旧库都能正常工作 ✅

## GitHub Actions 升级：自动版本号 + Release

- **`.github/workflows/build.yml`** 改名为 "Build & Release"
- 新增 `permissions: contents: write` 以允许创建 Release
- `checkout` 增加 `fetch-depth: 0` 以获取完整 git 历史
- 版本号格式：`v<YYYY.MM.DD>.<run_number>`（如 `v2026.03.17.42`）
- 自动生成 changelog：从上一个 tag 到当前 HEAD 的 commit 列表
- 使用 `softprops/action-gh-release@v2` 创建 GitHub Release，附带 `zeshawn-site.tar.gz` 产物
- Release body 包含：版本号、构建时间、触发提交、更新日志、产物说明
